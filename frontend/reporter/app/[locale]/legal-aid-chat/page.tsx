import Chat from "@/components/chatbot/chat";

export default function LegalAidChatPage() {
  return (
    <main className="h-screen w-screen flex flex-col overflow-hidden">
      <Chat />
    </main>
  );
}